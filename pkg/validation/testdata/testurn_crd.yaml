apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: testurn.tr.ccrn.example.com
  annotations:
    ccrn/v1.urn-template: "urn:ccrn:testurn.tr.ccrn.example.com/v1/<name>"
spec:
  group: tr.ccrn.example.com
  names:
    kind: TestURN
    plural: testurns
    singular: testurn
  scope: Namespaced
  versions:
    - name: v1
      served: true
      storage: true
      schema:
        openAPIV3Schema:
          type: object
          required:
            - name
          properties:
            name:
              type: string
              description: "The name of the resource"
              pattern: "^([a-z0-9]([-a-z0-9]*[a-z0-9])?|\\*)$"
              maxLength: 253

